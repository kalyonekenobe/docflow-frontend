import { FC } from 'react';
import { LogoIcon } from '../Icons/Icons';
import './App.css';

const App: FC = () => {
  return (
    <div>
      <LogoIcon className='size-8' /> Hello, world
    </div>
  );
};

export default App;
